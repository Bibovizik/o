import type { Control } from 'react-hook-form';
import type { PublishGameFormValues } from '../../../types/Game';

export interface PublishModalControlProps {
  control: Control<PublishGameFormValues>;
}